amends "pkl:project"

local githubUser = "BenMMcLean"

package {
  name = "pkl-upload-package"
  baseUri = "package://benm.cl/\(name)" 
  version = read("file:.version").text.trim()
  packageZipUrl = "https://benm.cl/\(name)/v\(version)/\(name)@\(version).zip"
}